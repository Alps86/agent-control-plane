# Planungsentscheidungen und Umgang mit dem Vorlauf

Stand: 24. September 2026. Diese Entscheidungen gelten für den Storyzuschnitt; ein technischer Nachweis oder eine Implementierungsabnahme wird damit nicht behauptet.

## Verbindliche Entscheidungen

| Thema | Entscheidung | Begründung und spätere Prüfung |
| --- | --- | --- |
| Ausgangsstand | Der frühere Commit `71ca16f` gehörte zum unvalidierten Vorlauf. Das frühere Git-Repository wurde vom Nutzer entfernt; seine Go- und UI-Fragmente sind gesichert und aus dem Arbeitsbaum entfernt. | Jede Story prüft betroffene Fragmente gegen aktuelle Fachabnahme, Architektur, Rechte und öffentliche Tests. Passende Teile können gezielt übernommen, sonst ersetzt werden. Ein Vergleich ist nur über die gesicherte historische Kopie möglich. |
| UI-Struktur | Die flache bisherige UI-Struktur wird bei den betroffenen Stories in die geplante Trennung von Go-Rendering, kleiner öffentlicher `ui/bridge`-Fassade, JS/CSS und JSON-Fixtures überführt. | Der Architekturvertrag in `architektur/umsetzungsplan.md` und `architektur/ui-vertrag.md` bestimmt konkrete Pfade. Vorschau liest ausschließlich JSON-Fixtures; Go-Templates bleiben lesbar eingerückt. |
| Ausführungsarten | Eino ist die primäre Ausführungsart; Codex CLI ist ein getrenntes Profil, das nur bei beweisbarer Fachfähigkeitsgrenze wählbar ist. Eino erhält den eigenen Chatmodell-Adapter mit Codex-Abo als Standard und bewusst wählbarem OpenRouter je Agent und Modell. | `MS-01` muss direkten Abo-Zugriff einschließlich Eino-Toolrunde real nachweisen; ein CLI-Lauf ist kein Ersatz. OpenRouter-Key wird in Settings verwaltet (`MS-03`). |
| Anbietergrenze | Settings verwaltet Codex-Gerätecode und zentralen OpenRouter-Key. Anbieterkennung, Authentifizierungsart, Verbindung, Modell und belegte Fähigkeiten kommen aus Konfiguration. Je Anbieter gibt es einen Eino-Chatmodell-Adapter; beim Laufstart wählt eine kleine mapbasierte Registry den ausdrücklich konfigurierten Adapter. Agentenablauf, App- und Domainregeln sowie Rechteprüfung bleiben anbieterneutral. | `MS-09` prüft die Erweiterung mit einem registrierten, extern konfigurierten Testanbieter über öffentliche Auswahl-, Lauf-, Rechte- und Fehlerfälle. Kein Gate für AGT-01, RUN-01 oder MS-01/05; kein generischer Plugin-Lader als Vorleistung. Gemini/Anthropic sind spätere Optionen ohne V1-, OAuth- oder Abo-Zusage. |
| Zentraler OpenRouter-Key und Organisationsrecht | Settings verwaltet mit `MS-03` einen serverweiten OpenRouter-Key. `MS-04` erteilt dessen Nutzung je Organisation und Agent ausdrücklich; jede daraus angebotene Verbindungsreferenz gehört genau einer Organisation. Key-Bestand, neue Organisationen, neue Agenten und Rotation erteilen keine automatische Freigabe. | Die Auflösung prüft Organisations- und Agentenrecht vor jedem neuen und noch nicht ausgeführten Aufruf. Entzug sperrt diese Aufrufe; `MS-05`, `MS-07`, `MS-08` und `PERM-01` übernehmen diese Grenze. |
| Kleine Lieferstories | `ARC-02` liefert nur die SQLite-Grundlage, Fachschemata kommen mit den jeweiligen Stories. `ARC-04` liefert die atomare Laufreservierung und sichtbare Persistenz; Crash-Fortsetzung und Cron-Regeln gehören zu `RUN-04` und `SCH-04`. `UI-04` beginnt mit der echten Organisationsansicht nach `ORG-01`. | `OPS-06` nimmt erst nach den POC-Fachstories den vollständigen Nutzungsweg nach Neustart ab. Die Bootstrap-Stories verlangen keine vorgezogenen Fachobjekte oder Scheduler-Regeln. |
| Modell-IDs | `MS-01` bis `MS-09` sind die kanonischen Modellstories. Die alten `MOD-01` bis `MOD-05` sind reine Verweisnamen, keine weiteren Liefer- oder Branch-Einheiten. | Zuordnung in `spec/planung/modelle-sprache/stories.md`; alle neuen Status- und Revieweinträge benutzen MS-IDs. |
| Sprache | Der belegbare Sprach-Turn und echte bidirektionale Echtzeit bleiben getrennte Abnahmen. | `VO-01` belegt konkrete OpenRouter-Modelle und Routen, `VO-07` den Realtime-Vertrag. Vor dessen Nachweis darf die UI nur den geprüften Turn-Modus anbieten. |
| Zusammenarbeit | Team-To-dos sind Sichten auf dieselben Issues. Jede Organisation konfiguriert zulässige Delegations- und Arbeitsabläufe ohne globale feste Prozesskette oder Workflow-DSL. Eino und die Anwendung validieren Zustände, Übergänge und Rechte; Sprach-, Chat- und UI-Aktionen laufen über dieselben Fachfälle. | `ZA-01` bis `ZA-07`; `CHV-01` ist der Vertrag zwischen Voice und Chat. Keine zweite Aufgabendomäne und keine Rechteausweitung bei Kinddelegation. |
| Anmeldung und Budget | Keine App-Anmeldung, Registrierung oder Budgetverwaltung in V1. Modellanbieter-Anmeldung bleibt notwendig und davon getrennt. | `OPS-05` prüft auch öffentliche Schnittstellen und Import/Export; fachliche Reviewentscheidungen bleiben. |
| Paperclip-Parität | Belegte Kernabläufe kommen in den Katalog; Alpha und Experimente werden in `EXP-01` bewertet. Weitere Provider und Connectors werden in `INT-05` einzeln entschieden. | Keine Gleichsetzung von Paperclip-Zieltext mit ausgelieferter Funktion und kein Versprechen aller Connectoren. |
| Git-Arbeit | Git ist derzeit nicht eingerichtet; der Nutzer legt das Repository später neu an. Nach vollständiger Planung und unabhängigem Planungsreview wird jede Entwicklungsstory erst nach dieser Git-Neuanlage in eigenem Branch und isoliertem Worktree bearbeitet. | Separater Git-Diff-Review, gezielte Befundkorrektur, zeitnaher Merge jeder fertigen Story und Statuspflege in `PLANUNG.md`; keine Sammelwartewelle. |

## Einheitliche Laufbeobachtung

Eino und ein zugelassenes Codex-CLI-Profil verwenden strukturierte Logs mit Level, Ereignis, Zeit, Organisation, Projekt, Aufgabe, Agent, Sitzung, Lauf, Versuch sowie tatsächlichem Provider und Modell. Regelmäßige Heartbeats zeigen nur die letzte belegte Phase und deren Zeit; erfundene Prozentwerte oder Modellfortschritte sind ausgeschlossen. SQLite speichert gemeldete Input-, Output- und Reasoning-Token je Aufgabe/Lauf/Versuch/Provider/Modell. Unbekannte, unvollständige oder geschätzte Werte bleiben kenntlich; die rohe Provider-Zählsemantik wird bewahrt und in Output enthaltenes Reasoning nicht doppelt gezählt. Streaming, Retry, Wiederaufnahme und Delegation brauchen idempotente Nutzungsereignisse; bei Abbruch bleibt bereits gemeldeter Verbrauch erhalten. Secrets, vollständige Prompts und Chain-of-Thought-Inhalte gelangen nicht in Logs oder Nutzungsdaten. Daraus entsteht kein Budget-, Abonnement- oder Kostenmodell.

## Sitzungen, Arbeitsprodukte und Skills

Organisationsbezogene Sitzungen und Arbeitskontexte bleiben dauerhaft und mit Herkunft nachvollziehbar. Modellgeneriertes Markdown ist nur Inhalt: Die Go-Anwendung schreibt es in einen begrenzten Organisations- und Sitzungs-Artefaktbereich; ein Rechercheagent darf nur Markdown-Arbeitsprodukte und keine ausführbaren Skripte oder Dateien erzeugen oder persistieren. Markdown wird nie ausgeführt. Skills und optionale `SOUL.md`-Verhaltensbeschreibungen erteilen keine Rechte. Script-Skills können nur vorab registrierte, geprüfte Fachaktionen per ID und validierten Parametern anfordern; Modell-Shelltext und generisches Exec sind ausgeschlossen. Websuche läuft ausschließlich über ein freigegebenes Eino-Tool und einen kontrollierten Go-Adapter, nicht als allgemeiner HTTP-/URL-Zugriff.

## Spätere Evaluation: Jev (TypeSafe)

[OpenRouter beschreibt Jev 1.13](https://openrouter.ai/typesafe/jev-1.13) als Modell für strukturierte Entscheidungen; sein [Tutorial](https://openrouter.ai/blog/tutorials/how-to-use-jev/) zeigt die `alpha`-Decisions-API mit vorgegebenen Antwortoptionen und Wahrscheinlichkeiten. [TypeSafe](https://docs.typesafe.ai/concepts/system-one) beschreibt diese Antworten als typisierte Entscheidungen statt generiertem Text. [Eino](https://www.cloudwego.io/docs/eino/overview/) dokumentiert ChatModel, Tools und Orchestrierung. Daraus folgt für diese Planung: Jev wäre eine gesonderte Entscheidungsanfrage, **kein** Eino-ChatModel-Adapter und kein Ersatz für Eino-Agentenläufe.

Erst später wird auf geeigneten, zulässig verwendbaren Projektdaten geprüft, ob Jev die Auswahl unter **vorab definierten und bereits erlaubten** Optionen sinnvoll verbessert, etwa bei Routing oder Klassifikation. Die Anwendung und Eino behalten Zustandsführung, Persistenz, Fachregeln und Rechteprüfung. Geringe Konfidenz, ungültige Antwort oder API-Fehler führen zur fachlich vorgesehenen Klärung beziehungsweise Blockade, ohne stillen Providerwechsel. Diese Evaluation ist kein V1-Gate und startet in der Planungsphase keine kostenpflichtigen Aufrufe; Nutzen, Schwellen und Kosten müssen vor einer Einführung anhand eigener Daten belegt werden.

## Technische Nachweisgrenzen

`MS-01`, `MS-06`, `VO-01` und `VO-07` führen reale Provider- und Routenbelege erst **nach** dem Planungsabschluss. Dokumentierte API, Gerätecode-Anmeldung, Modellkatalog und SSE sind jeweils nur Teilbelege. Ein unbekannter Transport oder fehlende Kontofreigabe wird als Blockade mit beobachtetem Ergebnis geführt, ohne stillen Anbieterwechsel.

Produktagenten einschließlich Codex CLI erhalten niemals direkten Shell-, Terminal-, Prozess-, Interpreter-, HTTP- oder Dateisystemzugriff. Eine generische Freigabe hebt diese Grenze nicht auf. Eino orchestriert nur benannte Fachfähigkeiten; die Go-Anwendung validiert Zustände, Rechte und Parameter und führt Wirkungen über Ports und Adapter aus. Codex CLI bleibt für ein Profil gesperrt, wenn seine tatsächlichen Tool- und Nebenwege diese Grenze nicht beweisbar einhalten. Kinddelegation darf Rechte nicht ausweiten. Daraus folgt kein pauschales Sandboxversprechen. `PERM-01`, `PERM-02`, `PERM-04` und `PERM-05` müssen die tatsächlichen Grenzen vor produktiven Agentenläufen beweisen. Secret-Speicherung und parallele Erneuerung gehören zu `MS-07` und `OPS-02`. Einzelne nicht idempotente externe Aktionen werden nach Abbruch nicht blind wiederholt (`ZA-06`, `RUN-04`).

Offene Anbieterfähigkeit bleibt ein technisches Gate; die übrigen Routineentscheidungen sind im Storykatalog ausreichend bestimmt, damit die spätere Umsetzung starten kann, sobald die Planungsprüfung abgeschlossen ist.
